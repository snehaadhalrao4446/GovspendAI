{
  "framework": "vite",
  "buildCommand": "pnpm build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [
    { "source": "/secure-gateway", "destination": "/api/health" },
    { "source": "/secure-gateway/(.*)", "destination": "/api/$1" }
  ]
}
